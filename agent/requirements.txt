# ITL.ControlPlane.Attestation Support Agent

# Azure AI SDK (for Foundry mode)
azure-ai-agents>=1.0.0
azure-identity>=1.25.3

# OpenAI SDK (for Ollama local mode)
openai>=2.37.0

# HTTP server (for Agent Inspector mode)
aiohttp>=3.13.5

# Configuration
python-dotenv>=1.2.2

# Debugging (optional - for VS Code integration)
debugpy>=1.8.21

# Agent Dev CLI (optional - for Agent Inspector)
# agent-dev-cli --pre
