name: CI — Pulumi Provider

# ---------------------------------------------------------------------------
# CI orchestrator for the Pulumi Provider (itl-attestation-pulumi on PyPI)
#
# Triggers on changes to src/pulumi_provider/** or src/sdk/** (depends on SDK).
# Follows the same release chain as ITL.BrainCell:
#   ci → auto-tag → create GitHub Release → publish.yml
# ---------------------------------------------------------------------------
on:
  push:
    branches: [main, develop, "feature/**", "release/**", "hotfix/**"]
    paths:
      - "src/pulumi_provider/**"
      - "src/sdk/**"
      - ".github/workflows/ci-pulumi.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "src/pulumi_provider/**"
      - "src/sdk/**"
  workflow_dispatch:

concurrency:
  group: ci-pulumi-${{ github.ref }}
  cancel-in-progress: true

jobs:

  detect-version:
    name: Detect version
    uses: ITlusions/ITL.Github/.github/workflows/_reusable-detect-version.yml@main
    with:
      tag-prefix: "pulumi/v"

  ci:
    name: Lint / Test / Build
    needs: detect-version
    uses: ITlusions/ITL.Github/.github/workflows/_reusable-ci-python.yml@main
    with:
      python-version: "3.12"
      ref: ${{ github.sha }}
      artifact-name: "pulumi-wheel"
      version: ${{ needs.detect-version.outputs.python-version }}
      working-directory: "src/pulumi_provider"
      ruff-src: "."
      test-path: "."
      extra-install: "pip install -e ../sdk && pip install -e .[dev]"

  build-meta:
    name: Compute build ID
    runs-on: ubuntu-latest
    outputs:
      build-id: ${{ steps.id.outputs.build-id }}
    steps:
      - name: Generate build ID
        id: id
        run: echo "build-id=$(date +%Y%m%d)-${{ github.run_number }}" >> "$GITHUB_OUTPUT"

  auto-tag:
    name: Auto-tag semver
    needs: ci
    if: |
      github.event_name == 'push' &&
      (
        github.ref == 'refs/heads/main' ||
        startsWith(github.ref, 'refs/heads/release/')
      )
    uses: ITlusions/ITL.Github/.github/workflows/_reusable-auto-tag.yml@main
    with:
      commit-sha: ${{ github.sha }}
      tag-prefix: "pulumi/v"
    secrets:
      gh-pat: ${{ secrets.GH_PAT }}

  create-release:
    name: Create GitHub Release
    needs: [auto-tag, build-meta]
    if: |
      github.event_name == 'push' &&
      (
        github.ref == 'refs/heads/main' ||
        startsWith(github.ref, 'refs/heads/release/')
      )
    uses: ITlusions/ITL.Github/.github/workflows/_reusable-release-gh.yml@main
    with:
      tag: ${{ needs.auto-tag.outputs.tag }}
      artifact-name: "pulumi-wheel"
      generate-release-notes: true
      body: |
        ## Installation

        ```bash
        pip install itl-attestation-pulumi==${{ needs.detect-version.outputs.python-version }}
        ```

        ---
        **Build:** ${{ needs.build-meta.outputs.build-id }}
        **Commit:** ${{ github.sha }}
        **Workflow run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
    secrets:
      gh-pat: ${{ secrets.GH_PAT }}